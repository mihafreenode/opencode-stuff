# Built-in service manifest: postgres

id: postgres
displayName: PostgreSQL
description: PostgreSQL service container for local development.
image: postgres:17
hostPorts:
  - 15432:5432
environment:
  POSTGRES_DB: app
  POSTGRES_USER: app
  POSTGRES_PASSWORD: app
volumes:
  - postgres-data:/var/lib/postgresql/data
