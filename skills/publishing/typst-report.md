# Typst Report Skill

Purpose:
Use Typst for modern, concise, AI-friendly document authoring when Markdown is not enough.

When to use:
- styled reports
- modern publishing templates
- easier-than-LaTeX document builds
