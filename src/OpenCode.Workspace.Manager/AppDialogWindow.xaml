<Window x:Class="OpenCode.Workspace.Manager.AppDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="520"
        SizeToContent="Height"
        MinHeight="220"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Icon="Assets/Icons/opencode-stuff-app.ico">
    <Border Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding Title}" FontSize="22" FontWeight="SemiBold" TextWrapping="Wrap" />
            <TextBlock Grid.Row="1"
                       Margin="0,16,0,0"
                       Text="{Binding Message}"
                       TextWrapping="Wrap"
                       xml:space="preserve" />
            <DockPanel Grid.Row="2" Margin="0,22,0,0" LastChildFill="False">
                <Button DockPanel.Dock="Right" Width="100" Height="36" Content="{Binding OkLabel}" Click="Ok_OnClick" Visibility="{Binding ShowOk, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Visibility="{Binding ShowYesNo, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Button Width="100" Height="36" Margin="0,0,10,0" Content="{Binding NoLabel}" Click="No_OnClick" />
                    <Button Width="100" Height="36" Style="{StaticResource PrimaryButtonStyle}" Content="{Binding YesLabel}" Click="Yes_OnClick" />
                </StackPanel>
            </DockPanel>
        </Grid>
    </Border>
</Window>
