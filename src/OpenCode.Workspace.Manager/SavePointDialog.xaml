<Window x:Class="OpenCode.Workspace.Manager.SavePointDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DialogTitle}"
        Width="700"
        Height="420"
        WindowStartupLocation="CenterOwner"
        Icon="Assets/Icons/opencode-stuff-app.ico">
    <Border Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock FontSize="22" FontWeight="SemiBold" Text="{Binding DialogTitle}" />
            <TextBlock Grid.Row="1" Margin="0,10,0,0" TextWrapping="Wrap" Text="{Binding DialogDescription}" />
            <TextBox Grid.Row="2"
                     Margin="0,16,0,0"
                     Text="{Binding SavePointMessage, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     TextWrapping="Wrap" />

            <DockPanel Grid.Row="3" Margin="0,16,0,0">
                <Button Width="110" Height="34" Content="{Binding CancelLabel}" Click="Cancel_OnClick" DockPanel.Dock="Right" />
                <Button Width="140" Height="34" Margin="0,0,10,0" Style="{StaticResource PrimaryButtonStyle}" Content="{Binding ConfirmLabel}" Click="Confirm_OnClick" DockPanel.Dock="Right" />
            </DockPanel>
        </Grid>
    </Border>
</Window>
